/** @jsx jsx */
import { jsx, css } from '@emotion/core'
import { } from '../index';
import Types from './types';

export default (props: Types.MenuProps) => {

    return (
        <div></div>
    );
}
