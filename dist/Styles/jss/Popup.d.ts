import { ThemeInterface } from "..";
declare const _default: (theme: ThemeInterface) => {
    color: string;
    shadowColor: string;
    borderColor: string;
    background: string;
};
export default _default;
