import { CSSProperties } from "../../../node_modules/@types/react";

interface Theme {
    colors: {
        pageBackground: string
        text: string
        pale: string
        uiBackground: string
        highlight: string
        accent: string
        red: string
    }
}

export default Theme